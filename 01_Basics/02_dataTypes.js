"use strict"; //basically it means that all the below javascript code is written in new JS lang //

// alert(3+3) it will still show error as we are writing in node not on browser //

let name = "Vedant" //String
let age = 19 //Number, there is one thing bigint to use big numbers
let isLoggedIn = true //Boolean
let state = null //null is not undefined it is empty
// symbol it is unique

console.log(typeof "Vedant" ) //to identify type of any thing