const totalSales = 1_50_00
let CEOofcompany = "Vedant Bhave"
// Var is not used now it is finally outdated//
/*Constant once declared are declare hence it will not be changed later*/

let revenue;
//If we just leave the variable after declaring it, and then print it will only show undefined// 


console.table([totalSales, CEOofcompany, revenue]);

/* console.table([]) it is used to print multiple things in tabular format once directly */